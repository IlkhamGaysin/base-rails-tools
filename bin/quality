#!/usr/bin/env sh

set -e

bin/rubocop
brakeman --quiet --skip-libs --exit-on-warn

# Using rake-task here since coffeelint.rb cmd doesn't exit with non-zero status
# when code contains errors
rails coffeelint
# bin/scss-lint
slim-lint app/views

bundle-audit update
bundle-audit
